bp(w = 705px, mobile_first = false)
  if (mobile_first == mf)
    unquote("@media all and (min-width: " + w + ") {")
  else
    unquote("@media all and (max-width: " + w + ") {")
endbp()
  unquote("}")

// alias
breakpoint = bp
