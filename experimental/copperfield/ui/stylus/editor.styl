body * { display: flex; flex-direction: column; }

pre { font-family: monospace; }
.ui-input { outline: none; }

// Debug
.view { border: 1px solid gray; }
.view div { display: block; }
.view span { display: inline-block; padding: 0 3px; }