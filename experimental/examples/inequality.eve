people older than 20
```
  match
    person = [#person age]
    age > 20
  bind
   person += #adult
```

inline people older than 20
```
  match
    [#person age > 20]
    really-old = is(age > 100)
    awesome = is(age = 50)
  bind
   person += #adult
```
