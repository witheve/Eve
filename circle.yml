machine:
  pre:
  - curl -sf https://raw.githubusercontent.com/brson/multirust/master/blastoff.sh | sudo sh
  - multirust default nightly-2015-04-20

test:
  pre:
  post:
    - pushd runtime && cargo test && popd