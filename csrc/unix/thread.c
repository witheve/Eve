#include <unix_internal.h>
#include <pthread.h>

static context io_thread;

static void io_thread(heap page_heap)
{
    c = init_context();
}

void start_io_thread(heap page_heap)
{
}

void asynch_write(buffer b, thunk finished)
{
}
